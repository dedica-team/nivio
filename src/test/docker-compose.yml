version: '3'

services:

  dld4e-headless:
    image: bonndan/dld4e-headless:latest
    ports:
      - 3030:3030
      - 9229:9229

  nivio:
    image: dedica/nivio:latest
    environment:
      DEMO: ${DEMO}
      SEED: ${SEED}
    ports: 
      - 8080:8080

