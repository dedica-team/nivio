export interface ILandscape {
    name: string;
    identifier: string;
    description?: string;
    contact?: string;
    teams: string[];
    groups?: string[];
    items?: string[];
    lastUpdate?: string;
}
